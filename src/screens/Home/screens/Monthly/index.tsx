import { SafeAreaView } from "react-native";
import { Text } from "react-native";

export default function Monthly()
{
    return (
        <SafeAreaView>
            <Text>Monthly</Text>
        </SafeAreaView>
    );
}