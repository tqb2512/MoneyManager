import { SafeAreaView } from "react-native";
import { Text } from "react-native";

export default function Celander()
{
    return (
        <SafeAreaView>
            <Text>Celander</Text>
        </SafeAreaView>
    );
}