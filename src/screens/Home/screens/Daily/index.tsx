import { SafeAreaView } from "react-native";
import { Text } from "react-native";

export default function Daily()
{
    return (
        <SafeAreaView>
            <Text>Daily</Text>
        </SafeAreaView>
    );
}